<% if(type==='esm'){ %>
import express from 'express';
import { create, update, remove, get, list } from '../controllers/<%= entity %>';
<% } else { %>
const express = require('express') ;
const { create, update, remove, get, list } = require('../controllers/<%= entity %>');
<% } %>
const router = express.Router();

router.post('/', create);
router.put('/', update);
router.delete('/', remove);
router.get('/', get);
router.get('/list', list);
<% if(type ==='esm') { %>
export default router;
<% } else { %>
module.exports = router;
<% } %>    